{
  "name": "amazon-events-render-target",
  "version": "1.0.0",
  "description": "Vamos a construir una tarea que parsea un target y permite modificarlo con valorex externos.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0"
  }
}
